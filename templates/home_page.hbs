<section class="hero">
  <div class="container hero__grid">
    <div>
      <h1>{{settings.hero_title}}</h1>
      <p>{{settings.hero_subtitle}}</p>
      <div class="hero__actions">
        <a class="btn btn--primary" href="{{help_center.url}}/hc/{{help_center.locale}}/requests/new">Falar com suporte</a>
        <a class="btn btn--ghost" href="{{help_center.url}}/hc/{{help_center.locale}}/search">Pesquisar artigos</a>
      </div>
    </div>

    <img class="hero__art" src="{{asset 'hero-air.svg'}}" alt="Equipe AIR colaborando">
  </div>
</section>

<section class="quick-links">
  <div class="container cards">
    <article class="card">
      <h3>Primeiros passos</h3>
      <p>Guias rápidos para configurar sua conta e começar a operar.</p>
    </article>

    <article class="card">
      <h3>Pagamentos e faturamento</h3>
      <p>Dúvidas sobre cobrança, notas e histórico financeiro.</p>
    </article>

    <article class="card">
      <h3>Status e incidentes</h3>
      <p>Acompanhe manutenção programada e disponibilidade da plataforma.</p>
    </article>
  </div>
</section>
